<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/stylesheets/style.css" />
    <title>Document</title>
</head>
<body>
    
    <h1>Edit <%= weapon.name %></h1>
    <form action="/weapons/<%=weapon._id%>?_method=PUT" method="POST">
        <label for="name">Name:</label>
        <input type="text" name="name" value="<%= weapon.name %>" />

        <label for="description">Description:</label>
        <input type="description" name="description" value="<%= weapon.description %>" />

        <div class="checkbox-div">
        <label for="isEquipped">Is it Equipped?</label>
        <input type="checkbox" name="isEquipped" <% if (weapon.isEquipped) { %>checked<% } %>>
        </div>

        <button type="submit">Update Weapon</button>
    </form>
 <a class="index-link-text" href="/weapons/">Back to Weapons</a>

</body>
</html>